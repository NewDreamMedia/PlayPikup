class ApiKeys {
  // Google Maps API Key - REPLACE WITH YOUR NEW KEY
  static const String googleMapsApiKey = 'YOUR_NEW_GOOGLE_API_KEY_HERE';
  
  // Google Places API Key (can be same as Maps key if enabled)
  static const String googlePlacesApiKey = 'YOUR_NEW_GOOGLE_API_KEY_HERE';
  
  // Mapbox Access Token
  static const String mapboxAccessToken = 'YOUR_MAPBOX_ACCESS_TOKEN';
  
  // CRITICAL: 
  // 1. The old key has been compromised and must be regenerated
  // 2. This file is now in .gitignore and won't be committed
  // 3. Use api_keys.dart.example as a template for other developers
}